const paths = require('./config/paths');
const source = require('./config/source');
const tasks = require('./config/tasks');
const port = 3000;

module.exports = {
    paths,
    source,
    tasks,
    port
};